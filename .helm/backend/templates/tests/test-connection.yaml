apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "g2o-backend.fullname" . }}-test-connection"
  labels:
    {{- include "g2o-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "g2o-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
